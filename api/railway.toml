[build]
builder = "NIXPACKS"
buildCommand = "cd api && go build -o bin/server ./cmd/server"

[deploy]
startCommand = "cd api && ./bin/server"
healthcheckPath = "/healthz"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
