[build]
builder = "NIXPACKS"
buildCommand = "cd web && npm install && npm run build"

[deploy]
startCommand = "cd web && npm run start -- --hostname 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
